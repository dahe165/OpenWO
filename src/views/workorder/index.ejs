<style>
.wo-header-main {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    flex: 1;
    min-width: 0;
}

.wo-header-icon {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    min-width: 34px;
    height: 34px;
    margin-top: 2px;
    font-size: 24px;
    line-height: 1;
}

.wo-header-main .wo-title-area {
    flex: 1;
    min-width: 0;
}

@media (max-width: 600px) {
    .wo-header-main { gap: 8px; }
    .wo-header-icon {
        width: 30px; min-width: 30px; height: 30px; font-size: 21px;
    }
}
</style>

<div class="container py-4">    

        <div class="mb-4">

        <h1 class="page-title">
            Work Order Saya
        </h1>

        <p class="page-subtitle">
            Pantau dan kerjakan Work Order yang ditugaskan kepada Anda.
        </p>

        </div>

        <%- include("../components/button/floating", {
            href: "/workorder/create",
            icon: "＋",
            text: "Buat Work Order",
            label: "Buat Work Order"
        }) %>

        <div class="search-box">

        <input
            id="searchInput"
            type="text"
            placeholder="🔍 Cari nomor WO atau judul..."
        >

        </div>

        <div class="filter-group">

            <button
                class="filter-btn <%= status === 'Semua' ? 'active' : '' %>"
                data-filter="Semua"
            >
                Semua
            </button>

            <button
                class="filter-btn <%= status === 'Menunggu' ? 'active' : '' %>"
                data-filter="Menunggu"
            >
                Menunggu
            </button>

            <button
                class="filter-btn <%= status === 'Diterima' ? 'active' : '' %>"
                data-filter="Diterima"
            >
                Diterima
            </button>

            <button
                class="filter-btn <%= status === 'Ditugaskan' ? 'active' : '' %>"
                data-filter="Ditugaskan"
            >
                Ditugaskan
            </button>

            <button
                class="filter-btn <%= status === 'Diproses' ? 'active' : '' %>"
                data-filter="Diproses"
            >
                Diproses
            </button>

            <button
                class="filter-btn <%= status === 'Waiting' ? 'active' : '' %>"
                data-filter="Waiting"
            >
                Waiting
            </button>

            <button
                class="filter-btn <%= status === 'Selesai' ? 'active' : '' %>"
                data-filter="Selesai"
            >
                Selesai
            </button>

        </div>

        <div id="wo-result-info" class="wo-result-info">

            <% if (pagination.totalItems > 0) { %>

                Menampilkan
                <strong>
                    <%= pagination.from %>–<%= pagination.to %>
                </strong>
                dari
                <strong>
                    <%= pagination.totalItems %>
                </strong>
                Work Order

            <% } else { %>

                Tidak ada Work Order ditemukan

            <% } %>

        </div>

    <div id="workorder-list">

        <div id="workorder-content">

        <% workorders.forEach((wo) => { %>

            <div class="wo-card <%= wo.id === activeId ? 'wo-detail' : 'wo-summary' %>" data-id="<%= wo.id %>" data-search="<%= (wo.nomor + ' ' + wo.judul).toLowerCase() %>" data-status="<%= wo.status %>">

            <div class="wo-header">

                <div class="wo-header-main">

                    <span class="wo-header-icon">
                        <%
                        let woIcon = "🛠️";

                        if (wo.kategori === "Incident") {
                            woIcon = "⚠️";
                        }

                        if (
                            wo.judul.toLowerCase().includes("internet") ||
                            wo.judul.toLowerCase().includes("wifi")
                        ) {
                            woIcon = "📡❌";
                        }

                        if (
                            wo.judul.toLowerCase().includes("lan") ||
                            wo.judul.toLowerCase().includes("jaringan")
                        ) {
                            woIcon = "🌐";
                        }

                        if (
                            wo.judul.toLowerCase().includes("komputer") ||
                            wo.judul.toLowerCase().includes("pc")
                        ) {
                            woIcon = "🖥️⚠️";
                        }

                        if (
                            wo.judul.toLowerCase().includes("printer")
                        ) {
                            woIcon = "🖨️";
                        }
                        %>
                        <%= woIcon %>
                    </span>

                    <div class="wo-title-area">

                        <div class="wo-title">

                            <span>
                                <%= wo.judul %>
                            </span>

                            <% if (wo.id !== activeId && wo.prioritas) { %>

                                <span class="wo-priority">
                                    · <%= wo.prioritas %>
                                </span>

                            <% } %>

                        </div>

                        <div class="wo-meta">

                            <span class="wo-number">
                                <%= wo.nomor %>
                            </span>

                            <span class="wo-meta-separator">
                                |
                            </span>

                            <span class="wo-category">
                                <%= wo.kategori %>
                            </span>

                            <span class="wo-meta-separator">
                                |
                            </span>

                            <span class="wo-created">
                                <%= new Date(
                                    wo.createdAt
                                ).toLocaleString(
                                    "id-ID",
                                    {
                                        day: "2-digit",
                                        month: "short",
                                        year: "numeric",
                                        hour: "2-digit",
                                        minute: "2-digit"
                                    }
                                ) %>
                            </span>

                        </div>

                    </div>

                </div>


                <%
                    let displayStatus =
                        wo.status;

                    if (
                        wo.status === "Diproses"
                    ) {
                        displayStatus =
                            "Sedang Dikerjakan";
                    }
                %>


                <div class="wo-current-status">

                    <span class="status-dot"></span>

                    <span>
                        <%= displayStatus %>
                    </span>

                </div>

            </div>

                    <% if (
                        wo.id === activeId &&
                        Number(wo.createdBy) !== Number(wo.pelaporId)
                    ) { %>

                        <div class="wo-person timeline-reporter">

                            <span class="wo-person-label">
                                Pelapor:
                            </span>

                            <strong>
                                <%= wo.pelapor || "-" %>
                            </strong>

                            <% if (
                                wo.pelaporSeksi ||
                                wo.pelaporBagian
                            ) { %>

                                <small class="wo-person-org">

                                    <% if (wo.pelaporSeksi) { %>
                                        <%= wo.pelaporSeksi %>
                                    <% } %>

                                    <% if (
                                        wo.pelaporSeksi &&
                                        wo.pelaporBagian
                                    ) { %>

                                        <span class="wo-org-separator">
                                            •
                                        </span>

                                    <% } %>

                                    <% if (wo.pelaporBagian) { %>
                                        <%= wo.pelaporBagian %>
                                    <% } %>

                                </small>

                            <% } %>

                        </div>

                    <% } %>

            <!------>

                <% if (wo.id === activeId) { %>

                    <%
                        const timelineEvents = wo.timeline || [];

                        const mainStages = [
                            "Dibuat",
                            "Diterima",
                            "Ditugaskan",
                            "Dikerjakan",
                            "Selesai"
                        ];

                        const verificationStages = [
                            "Verifikasi Asman",
                            "Menunggu Verifikasi Manager",
                            "Verifikasi Manager",
                            "Ditutup"
                        ];

                        function getEvent(stage) {
                            return timelineEvents.find(
                                event => event.status === stage
                            );
                        }

                        function getStepClass(stage) {

                            const event = getEvent(stage);

                            if (!event) {
                                return "pending";
                            }

                            const lastEvent =
                                timelineEvents[timelineEvents.length - 1];

                            if (event === lastEvent) {
                                return "active";
                            }

                            return "done";
                        }
                    %>

                    <!-- ================================= -->
                    <!-- TIMELINE PEKERJAAN -->
                    <!-- ================================= -->
                    
                    <div class="wo-timeline-section">

                        <div class="timeline-heading">
                            <span class="timeline-heading-icon">⚙️</span>
                            <span>Timeline</span>
                        </div>


                        <div class="timeline-main">

                            <% mainStages.forEach((stage) => {

                                const event = getEvent(stage);
                                const stepClass = getStepClass(stage);

                            %>

                                <div class="timeline-main-step <%= stepClass %>">

                                    <div class="timeline-step-label">
                                        <%= stage %>
                                    </div>

                                    <div class="timeline-dot">

                                        <% if (stepClass === "done") { %>
                                            ✓
                                        <% } else if (stepClass === "active") { %>
                                            ●
                                        <% } %>

                                    </div>

                                    <% if (event) { %>

                                        <div class="timeline-info">

                                            <strong>
                                                <%= event.user %>
                                            </strong>

                                            <small>
                                                <%= event.role %>
                                            </small>

                                            <% if (event.seksi || event.bagian) { %>

                                                <small class="timeline-org">

                                                    <% if (event.seksi) { %>
                                                        <%= event.seksi %>
                                                    <% } %>

                                                    <% if (event.seksi && event.bagian) { %>
                                                        <span class="timeline-org-separator">•</span>
                                                    <% } %>

                                                    <% if (event.bagian) { %>
                                                        <%= event.bagian %>
                                                    <% } %>

                                                </small>

                                            <% } %>

                                            <small>
                                                <%= new Date(
                                                    event.tanggal
                                                ).toLocaleString(
                                                    "id-ID",
                                                    {
                                                        day: "2-digit",
                                                        month: "short",
                                                        year: "numeric",
                                                        hour: "2-digit",
                                                        minute: "2-digit"
                                                    }
                                                ) %>
                                            </small>

                                        </div>

                                    <% } %>

                                </div>

                                <% if (
                                    stage === "Dikerjakan" &&
                                    wo.status === "Waiting"
                                ) { %>

                                    <form
                                        method="POST"
                                        action="/workorder/<%= wo.id %>/resume-waiting"
                                        class="timeline-waiting-form"
                                    >

                                        <button
                                            type="submit"
                                            class="timeline-waiting-button"
                                            title="Lanjutkan Pekerjaan"
                                            aria-label="Lanjutkan Pekerjaan"
                                        >
                                            ⏸
                                        </button>

                                    </form>

                                <% } %>

                            <% }) %>

                        </div>


                        <!-- ================================= -->
                        <!-- VERIFIKASI -->
                        <!-- ================================= -->

                        <div class="verification-flow">

                            <div class="verification-line"></div>

                            <div class="verification-header">

                                <span class="verification-icon">
                                    ✓
                                </span>

                                <div>

                                    <strong>
                                        Tahap Verifikasi
                                    </strong>

                                    <small>
                                        Pemeriksaan dan persetujuan penyelesaian WO
                                    </small>

                                </div>

                            </div>


                            <div class="verification-steps">

                                <% verificationStages.forEach((stage) => {

                                    const event = getEvent(stage);
                                    const stepClass = getStepClass(stage);

                                %>

                                    <div class="verification-step <%= stepClass %>">

                                        <div class="verification-dot">

                                            <% if (stepClass === "done") { %>
                                                ✓
                                            <% } else if (stepClass === "active") { %>
                                                ●
                                            <% } %>

                                        </div>


                                        <div class="verification-content">

                                            <strong>
                                                <%= stage %>
                                            </strong>


                                            <% if (event) { %>

                                                <div class="verification-user">

                                                    <span>
                                                        <%= event.user %>
                                                    </span>

                                                    <span>
                                                        <%= event.role %>
                                                    </span>

                                                </div>


                                                <small>

                                                    <%= new Date(
                                                        event.tanggal
                                                    ).toLocaleString(
                                                        "id-ID",
                                                        {
                                                            day: "2-digit",
                                                            month: "short",
                                                            year: "numeric",
                                                            hour: "2-digit",
                                                            minute: "2-digit"
                                                        }
                                                    ) %>

                                                </small>

                                            <% } else { %>

                                                <small>
                                                    Menunggu proses berikutnya
                                                </small>

                                            <% } %>

                                        </div>

                                    </div>

                                <% }) %>

                            </div>

                        </div>

                    </div>                    

                <% } else { %>

                    <div class="wo-footer">

                    <!--- <div>
                            Status :
                            <strong><%= wo.status %></strong>
                        </div>
                        <div>
                            <%= wo.update %>
                        </div> --->

                    </div>
                    
                <% } %>

                <!-- INFORMASI WO Tambahkan Pelapor & Teknisi -->

                <% if (wo.id === activeId) { %>
                    
                    <% if (wo.status === "Selesai") { %>

                    <!-- ================================= -->
                    <!-- HASIL PENYELESAIAN -->
                    <!-- ================================= -->

                    <div class="completion-result">

                        <div class="completion-result-header">

                            <span class="completion-result-icon">
                                📝
                            </span>

                            <div>

                                <strong>
                                    Hasil Penyelesaian
                                </strong>

                                <small>
                                    Laporan pekerjaan teknisi
                                </small>

                            </div>

                        </div>


                        <!-- ================================= -->
                        <!-- HASIL & FOTO -->
                        <!-- ================================= -->

                        <div class="completion-result-grid">


                            <!-- DESKRIPSI -->
                            <div class="completion-result-block">

                                <span class="result-label">
                                    Deskripsi Penyelesaian
                                </span>

                                <div class="completion-result-description">

                                    <% if (wo.resolutionDescription) { %>

                                        <p>
                                            <%= wo.resolutionDescription %>
                                        </p>

                                    <% } else { %>

                                        <p class="result-empty">
                                            Belum ada laporan penyelesaian.
                                        </p>

                                    <% } %>

                                </div>

                            </div>


                            <!-- FOTO -->
                            <div class="completion-result-block">

                                <span class="result-label">
                                    📷 Bukti Pekerjaan
                                </span>

                                <div class="completion-result-photo">

                                    <% if (wo.completionPhoto) { %>

                                        <img
                                            src="<%= wo.completionPhoto %>"
                                            alt="Foto penyelesaian"
                                            class="completion-photo-thumb"
                                        >

                                    <% } else { %>

                                        <div class="photo-empty">

                                            <span>📷</span>

                                            <small>
                                                Belum ada foto penyelesaian.
                                            </small>

                                        </div>

                                    <% } %>

                                </div>

                            </div>


                        </div>

                    </div>

                    <% } %>

                <% } else { %>

                    <!-- ================================= -->
                    <!-- INFORMASI RINGKASAN WO -->
                    <!-- ================================= -->

                    <div class="wo-assignment-info">

                    <!-- ============================= -->
                    <!-- PELAPOR -->
                    <!-- ============================= -->

                    <div class="wo-person">

                        <span class="wo-person-label">
                            Pelapor:
                        </span>

                        <strong>
                            <%= wo.pelapor || "-" %>
                        </strong>


                        <% if (
                            wo.pelaporSeksi ||
                            wo.pelaporBagian
                        ) { %>

                            <small class="wo-person-org">

                                <% if (wo.pelaporSeksi) { %>

                                    <%= wo.pelaporSeksi %>

                                <% } %>


                                <% if (
                                    wo.pelaporSeksi &&
                                    wo.pelaporBagian
                                ) { %>

                                    <span class="wo-org-separator">
                                        •
                                    </span>

                                <% } %>


                                <% if (wo.pelaporBagian) { %>

                                    <%= wo.pelaporBagian %>

                                <% } %>

                            </small>

                        <% } %>

                    </div>


                    <!-- ============================= -->
                    <!-- TEKNISI -->
                    <!-- ============================= -->

                    <div class="wo-person">

                        <span class="wo-person-label">
                            Teknisi:
                        </span>

                        <strong>
                            <%= wo.teknisi || "Belum ditugaskan" %>
                        </strong>


                        <% if (
                            wo.teknisiSeksi ||
                            wo.teknisiBagian
                        ) { %>

                            <small class="wo-person-org">

                                <% if (wo.teknisiSeksi) { %>

                                    <%= wo.teknisiSeksi %>

                                <% } %>


                                <% if (
                                    wo.teknisiSeksi &&
                                    wo.teknisiBagian
                                ) { %>

                                    <span class="wo-org-separator">
                                        •
                                    </span>

                                <% } %>


                                <% if (wo.teknisiBagian) { %>

                                    <%= wo.teknisiBagian %>

                                <% } %>

                            </small>

                        <% } %>

                    </div>

                </div>

                <% } %>                    

                <!-- AKSI WO -->

                <div class="wo-actions">                    
                    
                    <% if (
                        role === "asman" &&
                        wo.status === "Diterima"
                    ) { %>

                        <form
                            method="POST"
                            action="/workorder/<%= wo.id %>/assign"
                            class="assign-form"
                        >

                            <select
                                name="technicianId"
                                class="technician-select"
                                required
                            >

                                <option value="">
                                    Pilih Teknisi
                                </option>

                                <% technicians.forEach(technician => { %>

                                    <option
                                        value="<%= technician.id %>"
                                    >
                                        <%= technician.nama %>
                                    </option>

                                <% }) %>

                            </select>

                            <button
                                type="submit"
                                class="btn-action btn-assign"
                            >
                                ✓ Tugaskan Teknisi
                            </button>

                        </form>

                    <% } %>
                    
                    <!------>

                    <% if (
                        role === "asman" &&
                        wo.status === "Menunggu"
                    ) { %>

                        <form
                            method="POST"
                            action="/workorder/<%= wo.id %>/accept">

                            <button
                                type="submit"
                                class="btn-action btn-accept">

                                ✓ Terima WO
                            </button>
                        </form>

                    <% } %>

                    <% if (
                        role === "teknisi" &&
                        wo.status === "Ditugaskan"
                    ) { %>

                        <form
                            method="POST"
                            action="/workorder/<%= wo.id %>/start">

                            <button
                                type="submit"
                                class="btn-action btn-start">

                                ▶ Mulai Kerjakan
                            </button>
                        </form>

                    <% } %>


                    <% if (
                        role === "teknisi" &&
                        wo.status === "Diproses"
                    ) { %>

                        <form
                            method="POST"
                            action="/workorder/<%= wo.id %>/waiting"
                            class="waiting-form"
                        >

                            <button
                                type="submit"
                                class="btn-action btn-waiting"
                            >
                                ⏸ Waiting
                            </button>

                            <select
                                name="reason"
                                class="waiting-reason"
                                required
                            >

                                <option value="">
                                    ⏸ Pilih alasan menunggu...
                                </option>

                                <option value="Menunggu Barang">
                                    📦 Menunggu Barang
                                </option>

                                <option value="Menunggu User">
                                    👤 Menunggu User
                                </option>

                                <option value="Menunggu Vendor">
                                    🏢 Menunggu Vendor
                                </option>

                                <option value="Menunggu Keputusan">
                                    👔 Menunggu Keputusan
                                </option>

                                <option value="Lainnya">
                                    📝 Lainnya
                                </option>

                            </select>                          

                        </form>

                        <% if (
                            wo.id === activeId &&
                            completeMode
                        ) { %>

                            <form
                                method="POST"
                                action="/workorder/<%= wo.id %>/complete"
                                class="completion-form"
                                enctype="multipart/form-data"
                            >

                                <div class="completion-header">

                                    <span class="completion-icon">
                                        📝
                                    </span>

                                    <div>

                                        <strong>
                                            Penyelesaian Pekerjaan
                                        </strong>

                                        <small>
                                            Lengkapi hasil pekerjaan sebelum menyelesaikan Work Order.
                                        </small>

                                    </div>

                                </div>


                                <div class="completion-field">

                                    <label for="resolution-<%= wo.id %>">
                                        Deskripsi Penyelesaian
                                    </label>

                                    <textarea
                                        id="resolution-<%= wo.id %>"
                                        name="resolutionDescription"
                                        rows="4"
                                        placeholder="Jelaskan pekerjaan yang telah dilakukan..."
                                        required
                                    ></textarea>

                                </div>


                                <div class="completion-field">

                                    <label for="photo-<%= wo.id %>">
                                        📷 Foto Penyelesaian
                                    </label>

                                    <input
                                        type="file"
                                        id="photo-<%= wo.id %>"
                                        name="completionPhoto"
                                        accept="image/png,image/jpeg,image/webp"
                                    >

                                    <small class="field-hint">
                                        Tambahkan foto sebagai bukti penyelesaian.
                                    </small>

                                </div>


                                <div class="completion-actions">

                                    <button
                                        type="submit"
                                        class="btn-action btn-complete"
                                    >
                                        ✓ Selesaikan Work Order
                                    </button>

                                </div>

                            </form>

                        <% } else { %>

                            <button
                                type="button"
                                class="btn-action btn-complete btn-open-completion"
                                data-id="<%= wo.id %>"
                            >
                                ✓ Selesaikan
                            </button>

                        <% } %>

                    <% } %>

                    <% if (
                        role === "teknisi" &&
                        wo.status === "Waiting"
                    ) { %>

                        <form
                            method="POST"
                            action="/workorder/<%= wo.id %>/resume-waiting"
                        >

                            <button
                                type="submit"
                                class="btn-action btn-start"
                            >
                                ▶ Lanjutkan Pekerjaan
                            </button>

                        </form>

                    <% } %>

                    <% if (
                        wo.status === "Selesai" &&
                        role === "asman"
                    ) { %>

                        <div class="asman-verification-action">

                            <div class="asman-action-row">

                                <div class="asman-primary-action">

                                    <form
                                        method="POST"
                                        action="/workorder/<%= wo.id %>/verify"
                                        class="verify-form">

                                        <button
                                            type="submit"
                                            class="btn-action btn-verify">

                                            ✓ Verifikasi WO
                                        </button>
                                    </form>


                                    <button
                                        type="submit"
                                        form="<%= 'escalate-' + wo.id %>"
                                        class="btn-action btn-escalate-primary">
                                        
                                        ⚠ Eskalasi ke Manager
                                    </button>

                                </div>

                                <label class="escalation-toggle">

                                    <input
                                        type="checkbox"
                                        class="escalation-toggle-input">

                                    <span class="escalation-toggle-slider"></span>

                                    <span class="escalation-toggle-label">
                                        Eskalasi ke Manager
                                    </span>

                                </label>

                            </div>


                            <form
                                id="<%= 'escalate-' + wo.id %>"
                                method="POST"
                                action="/workorder/<%= wo.id %>/escalate"
                                class="escalate-form">

                                <textarea
                                    name="reason"
                                    class="escalate-reason"
                                    placeholder="Alasan eskalasi ke Manager..."
                                    required
                                ></textarea>
                            </form>

                        </div>

                    <% } %>                    

                    <% if (
                        role === "manager" &&
                        wo.status === "Menunggu Verifikasi Manager"
                    ) { %>

                        <div class="wo-actions">

                            <form
                                method="POST"
                                action="/workorder/<%= wo.id %>/verify-manager">

                                <button
                                    type="submit"
                                    class="btn-action btn-verify">

                                    ✓ Verifikasi Manager
                                </button>

                            </form>

                        </div>

                    <% } %>                    

                </div>

                <!-- FOOTER UNTUK DETAIL -->

                <% if (wo.id === activeId) { %>

                    <div class="wo-footer">
                        <!--
                        <div>
                            👨 <%= wo.pelapor %>
                        </div>
                        --->
                    </div>

                <% } %>

            </div>

        <% }) %>

        </div>

        <%
            const totalPages =
                pagination.totalPages;

            const currentPage =
                pagination.page;

            const paginationParams =
                new URLSearchParams();

            if (search) {

                paginationParams.set(
                    "search",
                    search
                );

            }

            if (
                status &&
                status !== "Semua"
            ) {

                paginationParams.set(
                    "status",
                    status
                );

            }


            /*
            * Membuat daftar halaman yang
            * perlu ditampilkan.
            *
            * null = ...
            */

            const pages = [];

            if (totalPages <= 7) {

                for (
                    let i = 1;
                    i <= totalPages;
                    i++
                ) {

                    pages.push(i);

                }

            } else {

                pages.push(1);


                /*
                * Bagian awal
                */

                if (currentPage <= 4) {

                    pages.push(2);
                    pages.push(3);
                    pages.push(4);
                    pages.push(5);

                    pages.push(null);

                }


                /*
                * Bagian tengah
                */

                else if (
                    currentPage >= 5 &&
                    currentPage <= totalPages - 4
                ) {

                    pages.push(null);

                    pages.push(
                        currentPage - 1
                    );

                    pages.push(
                        currentPage
                    );

                    pages.push(
                        currentPage + 1
                    );

                    pages.push(null);

                }


                /*
                * Bagian akhir
                */

                else {

                    pages.push(null);

                    pages.push(
                        totalPages - 4
                    );

                    pages.push(
                        totalPages - 3
                    );

                    pages.push(
                        totalPages - 2
                    );

                    pages.push(
                        totalPages - 1
                    );

                }


                pages.push(totalPages);

            }
        %>


        <% if (pagination && pagination.totalPages > 1) { %>

            <div class="wo-pagination">

                <!-- PREVIOUS -->

                <% if (currentPage > 1) { %>

                    <%
                        paginationParams.set(
                            "page",
                            currentPage - 1
                        );
                    %>

                    <a
                        href="/workorder?<%= paginationParams.toString() %>"
                        class="page-btn page-prev"
                        aria-label="Halaman sebelumnya"
                    >
                        ‹
                    </a>

                <% } %>


                <!-- PAGE NUMBERS -->

                <% pages.forEach(pageNumber => { %>

                    <% if (pageNumber === null) { %>

                        <span
                            class="page-ellipsis"
                        >
                            …
                        </span>

                    <% } else { %>

                        <%
                            paginationParams.set(
                                "page",
                                pageNumber
                            );
                        %>

                        <a
                            href="/workorder?<%= paginationParams.toString() %>"
                            class="page-btn <%= currentPage === pageNumber ? 'active' : '' %>"
                            <%= currentPage === pageNumber
                                ? 'aria-current="page"'
                                : ''
                            %>
                        >
                            <%= pageNumber %>
                        </a>

                    <% } %>

                <% }); %>


                <!-- NEXT -->

                <% if (currentPage < totalPages) { %>

                    <%
                        paginationParams.set(
                            "page",
                            currentPage + 1
                        );
                    %>

                    <a
                        href="/workorder?<%= paginationParams.toString() %>"
                        class="page-btn page-next"
                        aria-label="Halaman berikutnya"
                    >
                        ›
                    </a>

                <% } %>

            </div>

        <% } %>
    </div>
</div>